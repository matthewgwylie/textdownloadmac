reportlab>=4.0

# Enables --via-usb without Homebrew. Actively maintained and speaks
# the same iOS backup protocol as libimobiledevice. Invoked via
# `python3 -m pymobiledevice3` so it works even when pip's user-scripts
# directory isn't on PATH.
pymobiledevice3>=4.0

# Optional: enable inline rendering of HEIC images from newer iPhones.
# pillow>=10.0
# pillow-heif>=0.13
